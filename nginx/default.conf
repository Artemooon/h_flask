server {
    listen     80;
    server_name _;

    location ~ ^/(images|javascript|js|css|media|static)/{
        root    /home/artem/nginx/html;
        expires 30d;

        }

    location / {
        proxy_pass http://app:8000;
    }
}
